# تدارُك — Performance Hotfix (R58)

## الهدف
تحسين الأداء المحسوس بدون تغيير الهوية أو المحتوى أو بيانات المستخدم، مع الحفاظ على إصلاح ظهور المحتوى من أول دخول.

## ما تم

### 1) البحث الشامل
- قبل التعديل كان بناء البحث يقرأ 27 ملف JSON منفصلًا عند أول فتح للبحث.
- حجم هذه المصادر معًا يقارب 3.14 MiB قبل ضغط النقل.
- تم إنشاء `search-index.json` وقت البناء، ويحتوي على 1422 مدخلًا مختصرًا بحجم يقارب 429 KiB.
- البحث الآن يحتاج ملف فهرس واحدًا بدل قراءة 27 ملفًا كاملًا.
- بقي مسار fallback القديم في الكود: إذا تعذر فهرس البحث المسبق يمكن إعادة البناء من المصادر الأصلية بدل تعطيل البحث.

### 2) بداية التطبيق
- تم إيقاف تحميل/رسم الأذكار تلقائيًا أثناء فتح «يومي»؛ يتم تحميلها عند فتح قسم الأذكار، أو إعادة رسمها فقط إن كانت البيانات محملة بالفعل.
- قراءات الإعدادات والملف الشخصي وحالة onboarding أصبحت تتم بالتوازي بعد فحص سلامة البيانات بدل تنفيذها واحدًا بعد الآخر.
- تحميل «محفوظاتي» وبيانات المهام يبدأ بالتوازي قبل رسم يوم المستخدم.

### 3) قراءات التخزين المتكررة
- شريط الأيام السبعة أصبح يقرأ الأيام بالتوازي بدل سبع قراءات متسلسلة.
- بطاقة «أكمل من حيث توقفت» أصبحت تقرأ موضع المصحف/التزكية/فقه البوصلة بالتوازي، مع الحفاظ على نفس أولوية الاختيار السابقة.

### 4) PWA / cache
- رُفعت `cacheVersion` فقط إلى `20260823-r58-perf1` حتى لا تختلط ملفات الأداء الجديدة مع cache قديم.
- لم يتغير `appVersion` أو `dataSchema` أو بنية بيانات المستخدم.
- أضيف فهرس البحث إلى ملفات PWA المحلية.

## الاختبارات
- JavaScript syntax: PASS
- Static project validation: PASS — 0 warnings
- Performance budgets: PASS
- First-entry subpage regression: PASS — 23 hosts / 21 data files
- Performance static regression: PASS — 1422 search entries
- Release manifest rebuilt with SHA-256

## حدود التحقق
بيئة التنفيذ الحالية لا تسمح بتشغيل رحلة متصفح محلي end-to-end بسبب حظر localhost/file navigation، لذلك لا توجد أرقام زمنية حقيقية بالمللي ثانية من جهاز هاتف. التحسينات أعلاه مثبتة بنيويًا وباختبارات الملفات، ويجب تأكيد الأداء المحسوس على جهاز حقيقي بعد النشر.
