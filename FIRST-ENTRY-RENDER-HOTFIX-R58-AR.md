# تدارُك — إصلاح ظهور محتوى الأقسام من أول دخول

التاريخ: 2026-08-23
الأساس: R58 + UX Polish + Subpages + Stories

## المشكلة
عند فتح بعض الأبواب الداخلية في «العلم» كانت الصفحة المستقلة تظهر قبل اكتمال تحميل/رسم المحتوى، فيبدو الباب فارغًا. إذا خرج المستخدم ثم عاد، تكون البيانات قد اكتملت في الخلفية فيظهر المحتوى.

## السبب
- `renderKnowledge()` كان يبدأ بانتظار `knowledge.json` حتى للأبواب التي لا تحتاج هذا الملف.
- عدة renderers غير متزامنة كانت تُستدعى بدون `await` من الموجّه الرئيسي.
- لم تكن هناك حالة تحميل فورية موحدة للصفحة المستقلة.
- مع PWA يجب أيضًا تغيير `cacheVersion` عند هذا النوع من hotfix حتى لا تختلط ملفات تشغيل قديمة بالجديدة.

## الإصلاح
1. جعل `enterKnowledgeSubpage()` غير متزامن وينتظر `renderKnowledge()`.
2. إظهار الصفحة والـHeader وحالة «جارٍ تحميل المحتوى…» فور أول ضغطة.
3. إزالة الاعتماد غير الضروري على `knowledge.json` قبل رسم كل subpage؛ يُحمّل فقط عندما يحتاجه «ما لا يسع المسلم جهله/الفقه الميسر».
4. انتظار renderers الخاصة بالقصص، العقيدة، الفقه، السنة، السيرة، الصحابة، الأصول… بدل تشغيلها fire-and-forget.
5. إضافة حالة خطأ مرئية وزر «إعادة المحاولة» بدل ترك صفحة فارغة لو حدث خطأ غير متوقع.
6. رفع `cacheVersion` إلى `20260823-r58-renderfix1` لضمان تحديث ملفات PWA.
7. إضافة `tests/knowledge_first_render_static.py` كاختبار regression للمشكلة.

## التحقق
- JavaScript syntax: PASS
- Static project validation: PASS — 0 warnings
- Performance budgets: PASS
- Knowledge first-entry regression: PASS — 23 hosts / 21 data files
- E2E browser: لم يعمل داخل بيئة التنفيذ بسبب `ERR_BLOCKED_BY_ADMINISTRATOR` عند فتح خادم localhost، وهو قيد بيئة وليس نتيجة نجاح/فشل للتطبيق.

## ملاحظة اختبار مهمة على الجهاز
بعد رفع هذه النسخة، افتح مثلًا:
- العلم → السنة والسيرة → الأربعون النووية
- العلم → القرآن وعلومه → قصص الأنبياء
- العلم → العقيدة والإيمان → شرح العقيدة

المحتوى يجب أن يظهر من **أول دخول**؛ قد تظهر لحظيًا عبارة «جارٍ تحميل المحتوى…» في أول مرة فقط، ولا يجب أن تحتاج للخروج وإعادة الدخول.
