# مواصفة تنفيذية — الملف الشخصي، مجاهدة النفس، اختيار الثغر

**الحالة:** مصمَّمة بالكامل، غير منفَّذة. لم يُكتب محتوى شرعي بعد.

**العلاقة بين البنود الثلاثة:** الأسئلة الافتتاحية تُنتج الملف الشخصي ← الملف يحدّد ما يُعرض في «مجاهدة النفس» و«اختيار الثغر». فالبند الأول شرط للاثنين بعده ويُنفَّذ أولًا.

---

# البند الأول — توسيع الملف الشخصي

## الوضع القائم (متحقَّق منه من الكود)

`profile-v1` يحمل ثلاثة حقول فقط:

```js
profile = { age: null, gender: null, advancedIssues: false }
```

والافتتاح متعدد الخطوات موجود بالفعل (`onboarding-next` / `onboarding-prev` / `onboarding-seen-v`)، فالمطلوب توسيع لا بناء من الصفر.

## الحقول الجديدة

```js
profile = {
  age, gender, advancedIssues,   // قائم
  married: null,                 // true | false | null (تخطّى)
  hasKids: null,                 // يُسأل فقط إذا married === true
  timeBand: null,                // 'scarce' | 'moderate' | 'free'
  moneyBand: null,               // 'tight' | 'ok' | 'able'
  skills: []                     // اختيار متعدد
}
```

## قواعد إلزامية

**1.1 كل سؤال قابل للتخطّي.** زر «أفضّل عدم الإجابة» في كل خطوة، والقيمة تبقى `null`. من يتخطّى يحصل على تجربة كاملة لا ناقصة — يُعرض له المحتوى العام.

**1.2 لا يُسأل عن الأولاد إلا للمتزوج.** سؤال غير المتزوج عن أولاده خطأ فادح في السياق.

**1.3 صياغة محايدة.** «هل أنت متزوج؟» لا «هل أعانك الله بالزواج؟». غير المتزوج قد يكون في ظرف مؤلم، والصياغة الموحية بالفضل تجرح.

**1.4 نطاقات لا أرقام.** لا سؤال عن الدخل بالجنيه ولا عن الوقت بالساعة. نطاقات وصفية فقط.

**1.5 ترحيل آمن.** المستخدمون الحاليون لديهم ثلاثة حقول. الجديدة تُقرأ بـ `?? null` ولا يُعاد عرض الافتتاح عليهم؛ تُتاح من الإعدادات بدل الإجبار.

**1.6 لا خروج للبيانات.** كل هذا محلي كبقية المشروع، ويستحق سطرًا صريحًا للمستخدم عند السؤال.

**1.7 مراجعة `DATA_SAFETY.md`** قبل النشر، فإضافة حقول شخصية قد تمسّ إقرار Google Play.

---

# البند الثاني — عقبة «مجاهدة النفس»

## الموضع

عقبة جديدة في `obstacles` بنفس المخطط القائم:

```js
{ id:'mujahada', name:'مجاهدة النفس',
  sub:'ما يشدّك للخلف، وكيف تتحرر منه', items:[…] }
```

**تنبيه:** عقبة «الفتن الرَّقمية» قائمة وفيها ثلاثة عناصر عن الهاتف والمدخلات. يُراجَع محتواها أولًا؛ ما يتداخل يُربط لا يُكرَّر.

## الموضوعات المطلوبة

1. التدخين والنيكوتين (شامل الشيشة والفيب)
2. الأغاني والمسلسلات
3. انعدام الهوية
4. الهدف غير الواضح

ويُضاف لاحقًا بإقرار صاحب المشروع: الإباحية، العلاقات المحرّمة، رفقة السوء، تضييع الوقت.

## مخطط العنصر — القائم مع إضافتين

العنصر يتبع مخطط `obstacles[].items` الحالي: `id, q, why, answer, answerSource, steps, sources, depthId, depthType, depthParent, levels`. والإضافتان:

```js
weight: {                       // درجة العقبة وحجمها في حياة المستخدم
  q: 'كم مرة تجد نفسك فيها…؟',
  options: [
    { t:'نادرًا',         v:1, sayTo:'ليست عقبة عندك اليوم؛ خذ ما يحفظك.' },
    { t:'أحيانًا',        v:2, sayTo:'العادة في أولها، والآن أسهل وقت لقطعها.' },
    { t:'كثيرًا',         v:3, sayTo:'العقبة قائمة وتحتاج خطة لا نية فقط.' },
    { t:'يوميًا تقريبًا', v:4, sayTo:'الأثر متجذّر؛ تدرّج ولا تطلب القطع دفعة واحدة.' }
  ]
},
plan: {                         // تصوّر كامل لأصل المشكلة وعلاجها على مراحل
  origin: { t:'أصل المشكلة…', s:{…} },   // لماذا تحدث، لا مجرد أنها محرّمة
  stages: [
    { n:1, title:'الوعي',     tx:'…', s:{…} },
    { n:2, title:'التقليل',   tx:'…', s:{…} },
    { n:3, title:'الاستبدال', tx:'…', s:{…} },
    { n:4, title:'الثبات',    tx:'…', s:{…} }
  ]
}
```

**الدرجة تُخزَّن في مفتاح جديد** `mujahada-weight-v1` بشكل `{ itemId: value }`، ولا تُكتب في `profile-v1`.

## قواعد الصياغة — غير قابلة للتفاوض

**2.1 التقدير أداة توجيه لا حكم.** الدرجة تغيّر ما يُعرض فقط. **يُمنع** عرض مجموع أو نسبة مئوية أو تصنيف للشخص («مستواك: ضعيف») أو أي شيء يُقرأ كحكم على الإيمان — امتداد مباشر للقسم 2.9.

**2.2 لا تقريع.** من يفتح هذا القسم يعرف أن ما يفعله خطأ؛ يحتاج مخرجًا لا تذكيرًا بذنبه.

**2.3 لا تفاصيل تُغري.** لا وصف للمشاهد ولا طرق وصول ولا أسماء تطبيقات أو أعمال بعينها، ولو في سياق التحذير.

**2.4 الاعتراف بالبُعد الإدماني.** النيكوتين له أثر عصبي حقيقي؛ التعامل معه كضعف إرادة فقط يُفشل المستخدم ثم يُشعره بأنه ميؤوس منه. يُذكر بوضوح واعتدال بلا مبالغة طبية.

**2.5 التوجيه للمختص إلزامي** في كل موضوع ذي بُعد إدماني، بإعادة استخدام صياغة `meta.medicalNote` القائمة لا باختراع صياغة موازية.

**2.6 لا برامج زمنية مخترعة** — لا «١٠ أيام للتعافي» ولا «٤٠ يومًا» (القسم 2.12).

**2.7 الخلاف الفقهي المعتبر يُعرض كخلاف.** مسألة الأغاني فيها خلاف بين أهل العلم؛ تُعرض الأقوال بمصادرها ولا يُفرض قول واحد كأنه إجماع. هذا صدق علمي وحماية لثقة المستخدم.

**2.8 «انعدام الهوية» و«الهدف غير الواضح» ليسا معصية.** يُصاغان كحيرة يُعان عليها لا ذنبًا يُلام عليه، وإلا انقلب القسم إلى تأنيب.

---

# البند الثالث — «العمل: اختيار الثغر» في تبويب التزكية

## الفكرة

بند جديد يساعد المستخدم على اختيار ثغر نافع للمسلمين يناسب طاقته ووقته وعمره وحاله المادي — بصيغة أسئلة تنتهي بترشيحات، لا قائمة عامة يقرؤها ولا يفعل شيئًا.

## آلية العمل

يُقرأ من `profile-v1` ما هو معروف (`age`, `gender`, `married`, `hasKids`, `timeBand`, `moneyBand`, `skills`) فلا يُسأل المستخدم مرتين، ويُسأل فقط عن الناقص. النتيجة **ثلاثة ثغور مرشَّحة**، لكل منها خطوة أولى ملموسة تُنجَز هذا الأسبوع.

## أمثلة على منطق المواءمة

| الحال | ثغور مناسبة |
|---|---|
| وقت ضيق + قدرة مادية | كفالة، سقيا، دعم طالب علم |
| وقت متاح + شاب أعزب | تعليم الصغار، خدمة المسجد، تطوع ميداني |
| متزوج وله أولاد | تربية الأولاد نفسها باب عظيم — تُذكر صراحة لا تُهمَل |
| مهارة تقنية أو كتابية | محتوى نافع، تفريغ دروس، ترجمة، دعم تقني لمشاريع خيرية |
| طالب علم | إقراء القرآن، مراجعة محتوى، تدريس مبتدئين |

## قواعد

**3.1 كل ثغر ينتهي بخطوة أولى ملموسة** — «اسأل أقرب حلقة تحفيظ عن حاجتهم» لا «انفع المسلمين».

**3.2 المرأة والمتزوجة** تُقدَّم لها ثغور تناسب واقعها بلا حصر مُهين ولا تجاهل؛ تربية الأولاد وخدمة المحيط الأسري ثغور حقيقية.

**3.3 لا مقارنة ولا ترتيب فضل بين الثغور.** الأفضل ما يقدر عليه صاحبه ويداوم عليه.

**3.4 لا التزامات مالية على غير القادر.** `moneyBand === 'tight'` يستبعد الترشيحات المالية تمامًا.

**3.5 لا جهات بعينها.** لا أسماء جمعيات أو منصات؛ يتغيّر حالها ولا يمكن ضمانه، ويُعرّض المشروع للمساءلة.

**3.6 مصدر لكل تأصيل شرعي** — فضل نفع الناس وتعليم القرآن وكفالة اليتيم منصوص عليها وتُوثَّق كغيرها.

---

# قواعد المصادر — لكل ما سبق

نفس منهج R24 حرفيًا:

- كل معلومة شرعية لها مصدر بحقلَي `t` (الكتاب والجزء والصفحة) و`u` (رابط تحقّق).
- **المقبول:** الدرر السنية (الموسوعة العقدية، موسوعة الأخلاق، الموسوعة الحديثية)، ومواقع الفتوى الرسمية المعتمدة. **المرفوض:** المنتديات ومواقع التجميع التي تنقل بلا إسناد.
- **يُمنع منعًا باتًا** كتابة حديث أو أثر أو قول سلف من ذاكرة النموذج. يُفتح المصدر ويُقرأ ويُطابق رقم الصفحة قبل الكتابة.
- المادة الطبية عن النيكوتين تُصاغ عامة بلا أرقام دقيقة إلا من مصدر صحي معتبر.

---

# ترتيب التنفيذ

1. **حقول الملف الشخصي** — كود فقط بلا محتوى شرعي، وهي شرط للبندين الآخرين.
2. **موضوع «التدخين» وحده كاملًا** بمصادره، ليحكم صاحب المشروع على الأسلوب والعمق قبل التوسّع.
3. بقية موضوعات مجاهدة النفس بعد الإقرار.
4. **اختيار الثغر** أخيرًا لاعتماده على اكتمال الملف الشخصي.

# تنبيهات تقنية

- أي تعديل على `qalb.json` يستلزم رفع `CACHE_NAME` في `sw.js` (نتيجة تغيير R20).
- `mujahada-weight-v1` مفتاح جديد مستقل؛ لا يُدمج في `profile-v1` ولا يُعاد تسمية أي مفتاح قائم.
- تُسلَّم الحزمة باسم `PREVIEW` لأن التغيير يظهر في الواجهة ولن يكون قد عُوين في متصفح.


---
## R32 implementation status — 2026-08-20
The specification above is now implemented in the R32 source: optional profile context fields, all four requested Mujahada topics with local impact selection and sourced 4-stage plans, and the `العمل والنفع` recommendation flow. The implementation additionally adds `فقه البوصلة` in Knowledge, per the owner's subsequent request. See `CONTENT_PROVENANCE-FIQH-BUSOLA.md` and `CONTENT_PROVENANCE-MUJAHADA-WORK.md`.
