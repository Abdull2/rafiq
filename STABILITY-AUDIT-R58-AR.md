# تقرير الثبات والهندسة — تدارُك R58

## الهدف
R58 إصدار ثبات. لم يُعد بناء التطبيق من الصفر ولم تُغيّر بنية المحتوى العلمي في R57. الهدف تقليل احتمال أن يكسر تعديل صغير أجزاء بعيدة من التطبيق، وحماية بيانات المستخدم، وجعل كل إصدار قابلًا للفحص قبل النشر.

## 1. الإصدار المركزي
أضيف `version.js` ويحتوي على إصدار التطبيق والإصدار المنطقي ومخطط البيانات وإصدار الكاش. يعتمد عليه Backup وService Worker ورسائل الملاحظات بدل تكرار أرقام قد تنفصل عن بعضها.

## 2. التخزين
أضيف `storage.js`:
- IndexedDB هو المخزن الحديث الأساسي عند توفره.
- localStorage يبقى مرآة توافق حتى لا تضيع بيانات الإصدارات القديمة أو تتعطل أجزاء قديمة.
- القراءة من بيانات localStorage القديمة تنقلها تدريجيًا إلى IndexedDB.
- بيئات المضيف التي توفر `window.storage` تبقى مدعومة.
- واجهة التخزين موحدة: get/set/keys/remove/rawRead/rawSet.

## 3. حماية البيانات والنسخ الاحتياطية
- مخطط البيانات أصبح 3.
- الترحيل 2 -> 3 إضافي وغير هدّام.
- أضيف `mushafZoom` إلى البيانات الخام التي تدخل النسخة الاحتياطية.
- صيغة التصدير الجديدة `tadaruq-backup`، مع قبول `rafiq-backup` القديمة حفاظًا على التوافق.
- `TadaruqDataSafety` هو الاسم الجديد للواجهة العامة، مع إبقاء `RafiqDataSafety` كاسم توافق.
- فحص SHA-256 يمنع استيراد ملف تم تغييره إذا كان checksum متاحًا.
- الاستيراد يأخذ Snapshot قبل الكتابة ويعمل Rollback عند الفشل.

> ملاحظة توافق: بعض مفاتيح التخزين القديمة تبدأ بـ `rafiq:`. لم تُغيّر عمدًا لأن تغيير أسماء المفاتيح بلا ضرورة قد يعرّض بيانات مستخدمين قدامى للخطر.

## 4. PWA وOffline
أعيد بناء سياسة `sw.js`:
- Shell حرج يجب أن ينجح قبل تثبيت الإصدار.
- المحتوى الكبير ملفات اختيارية؛ فشل ملف واحد لا يسقط التثبيت كله.
- JSON يستخدم stale-while-revalidate.
- ملفات التطبيق المحلية تستخدم cache-first.
- Navigation يستخدم network-first مع fallback للكاش.
- المصادر الخارجية لا تُخزّن عشوائيًا؛ التخزين مقصور على مجموعات المحتوى المعروفة للمصحف/التفسير/الحديث.
- حدود لعدد عناصر الكاش التشغيلي.
- لا يوجد `skipWaiting()` إجباري أثناء install.
- المستخدم يختار «تحديث الآن» ثم يحصل reload مرة واحدة بعد تفعيل النسخة الجديدة.

## 5. الأمان
- أضيف Content Security Policy أساسي.
- `object-src 'none'` و`base-uri 'self'` و`form-action 'self'`.
- `connect-src` مقصور على التطبيق والنطاقات التي تستخدمها APIs الفعلية.
- Referrer Policy مضبوط على `strict-origin-when-cross-origin`.
- لم يُفعّل Trusted Types في R58 لأن التطبيق ما زال يحتوي عددًا كبيرًا من قوالب `innerHTML`؛ فرضه دفعة واحدة سيخلق مخاطرة انقطاع. هذه نقطة مرحلة لاحقة بعد تقليل مواضع HTML الديناميكي.

## 6. التشخيص
أضيف `diagnostics.js` وشاشة «حالة النظام» داخل الإعدادات. تعرض محليًا:
- نسخة التطبيق.
- Backend التخزين.
- نسخة مخطط البيانات.
- حالة Service Worker.
- عدد مخازن Cache Storage.
- تقدير مساحة التخزين.
- عدد أخطاء JavaScript في الجلسة إن وجدت.
لا ترسل هذه البيانات إلى خادم.

## 7. الاختبارات الآلية
`tools/validate_project.py` يفحص:
- وجود الملفات الحرجة.
- صحة JSON والـmanifest.
- تكرار IDs داخل القوائم.
- تكرار HTML IDs.
- الملفات المحلية المشار إليها من href/src/fetch/manifest.
- HTTPS للمراجع الخارجية المخزنة في البيانات.
- rel=noopener للروابط الثابتة target=_blank.
- وجود CSP.
- اتساق الإصدار المركزي.
- عدم فرض `skipWaiting` في مرحلة install.

`tools/check_budgets.py` يمنع تضخمًا غير مرئي في الملفات الأساسية.

`tests/e2e_smoke.py` يشغّل Chromium فعليًا ويختبر Desktop وMobile:
- فتح التطبيق دون أخطاء JavaScript.
- التنقل الرئيسي.
- الثيمات الثمانية.
- فحص البيانات والتشخيص.
- ترحيل بيانات قديمة من schema 2 إلى 3.
- انتقال التخزين إلى IndexedDB.
- إنشاء Backup ثم تعديل البيانات ثم Restore واسترجاع القيمة الأصلية.
- رفض Backup تم التلاعب به.
- تثبيت Service Worker.
- Reload بدون إنترنت.
- الصفحات المساندة Offline.
- حد أدنى لأهداف اللمس في شريط التنقل وعدم وجود overflow أفقي عام على شاشة الهاتف المختبرة.

## 8. نتائج R58 قبل التسليم
- Static validation: PASS.
- JavaScript syntax: PASS.
- Performance budgets: PASS.
- E2E Desktop: PASS.
- E2E Mobile: PASS.
- Storage migration: PASS.
- Backup/restore/checksum: PASS.
- Service Worker + Offline reload: PASS.

أرقام الفحص وقت الإصدار:
- 32 ملف JSON/manifest (منها release-manifest؛ و31 ملف بيانات/manifest للتطبيق).
- 2940 موضع URL مصدر/مرجع في البيانات التي فحصها المدقق البنيوي.
- نحو 6961 كائن محتوى شبيه بالمادة العلمية/التعليمية حسب القياس البنيوي، وهو قياس تقني لا يعني 6961 «مقالة» مستقلة.
- Critical code shell نحو 610 KiB، تحت ميزانية R58 البالغة 800 KiB.
- JSON corpus نحو 4.38 MiB، تحت ميزانية 6 MiB.

## 9. ما لم أفعله عمدًا في R58
- لم أنقل التطبيق إلى React/Flutter أو Framework جديد.
- لم أقسم `app.js` بالكامل في إصدار واحد؛ بدأ الفصل بالأجزاء الحساسة: version/storage/diagnostics. تفكيك بقية التطبيق يجب أن يكون تدريجيًا مع اختبارات كي لا يصبح «تنظيف الكود» سببًا لانكسار المنتج.
- لم أفرض schema جديدًا على آلاف عناصر المحتوى دفعة واحدة. مدقق المصادر الحالي خطوة تمهيدية، ويمكن في R59 إضافة Content Schema أكثر صرامة قسمًا قسمًا.
- لم أفحص وصول كل رابط خارجي عبر الشبكة في CI لأن المواقع الخارجية قد تمنع الروبوتات أو تتعطل مؤقتًا، ما يجعل النشر يفشل لأسباب ليست من التطبيق. الفحص الحالي يتحقق من البنية وHTTPS؛ فحص صحة الروابط الخارجية دوري منفصل أنسب.

## 10. المرحلة المقترحة التالية
R59 Content Integrity + Modularization:
1. تعريف schema رسمي للمحتوى والمصدر وتاريخ المراجعة.
2. ترحيل الأقسام تدريجيًا إلى schema دون تغيير النص العلمي.
3. استخراج وحدات المصحف، الأذكار، العلم، التزكية من `app.js` مع E2E لكل وحدة.
4. تحسين CSP بعد تقليل `innerHTML`، وربما Trusted Types في مرحلة لاحقة.
5. إضافة staging workflow قبل النشر الإنتاجي إذا أصبح المستودع يستخدم GitHub Actions للنشر.
